# erc20-token

```sh
# compile contracts
hardhat compile
# execute tests
hardhat test
# run scripts
hardhat run scripts/deploy.ts --network [network]
# verify
hardhat verify --network [contract-address]
```